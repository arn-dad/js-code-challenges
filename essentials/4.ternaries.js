// Ternaries

// 1. Write a ternary expression that checks if a number is positive, negative, or zero.

const number = -5;
let message = null;

console.log(message); // Output: The number is negative

// 2. Given a variable isWeekend, write a ternary expression that assigns a string "It's the weekend!" if isWeekend is true, otherwise assigns "It's a weekday".

const isWeekend = false;
let text = null;

console.log(text); // Output: It's a weekday