// Promise

// 1: Create a Promise that Resolves After a Delay

function delay(ms) {
    // code here
}

// Usage
delay(2000).then((message) => console.log(message)); // Output: Resolved after 2000 milliseconds
