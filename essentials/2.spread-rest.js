// Spread/Rest Operators

// 1. Extract the first two numbers, and store the remaining numbers in a separate array.

const numbers = [1, 2, 3, 4, 5];

// Expected Output:

console.log(first);  // 1
console.log(second); // 2
console.log(rest);   // [3, 4, 5]

// 2. You are given two arrays. Combine them into one array using the spread operator.

const arr1 = [1, 2, 3];
const arr2 = [4, 5, 6];


console.log(combinedArray); // [1, 2, 3, 4, 5, 6]


// 3. You are given two objects. Merge them into one object using the spread operator.

const obj1 = { name: 'Alice', age: 25 };
const obj2 = { city: 'Paris', country: 'France' };


console.log(mergedObj); // { name: 'Alice', age: 25, city: 'Paris', country: 'France' }

